export type meType = {
  error?: string;
  username?: string;
  email?: string;
} | null;

export type productType = {
  id: string;
  name: string;
  category: string;
};

export type categoryType = {
  id: string;
  name: string;
};
